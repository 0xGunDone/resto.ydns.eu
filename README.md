# Resto Management - Онлайн сервис для управления ресторанами

Полнофункциональная система управления ресторанами с поддержкой графиков работы, обмена сменами, задач, табелей, обратной связи и интеграцией с Telegram.

## Технологический стек

### Backend
- Node.js + Express + TypeScript
- PostgreSQL (pg)
- JWT аутентификация + 2FA
- Multer для загрузки файлов
- QR-коды для задач
- Telegram Bot API (Telegraf)
- Web Push уведомления

### Frontend
- React + TypeScript + Vite
- Tailwind CSS
- React Router
- Zustand для state management
- React Query для данных
- React Hook Form + Zod для валидации

## Быстрый старт

### Установка и запуск

```bash
# 1. Установите зависимости
npm install

# 2. Настройте PostgreSQL
# macOS:
brew install postgresql@14
brew services start postgresql@14
createdb resto

# 3. Настройте .env для backend
cd backend
cp .env.example .env
# Отредактируйте DATABASE_URL в .env

# 4. Инициализируйте БД
psql -d resto -f src/database/scripts/postgresql-schema.sql
npm run db:init

# 5. Запустите backend (из папки backend)
npm run dev

# 6. Запустите frontend (из папки frontend, в новом терминале)
cd ../frontend
npm run dev
```

**Вход:** http://localhost:5173
- Email: `admin@resto.local`
- Пароль: `Admin123!`

## Основные функции

- ✅ **Авторизация и роли** - JWT, 2FA для владельцев/менеджеров
- ✅ **Графики работы** - табличный график, типы смен, мультивыбор
- ✅ **Обмен сменами** - запросы на обмен, одобрение менеджером, уведомления
- ✅ **Система задач** - Kanban-доска, категории, статусы, файлы, QR-коды
- ✅ **Табель рабочего времени** - автоматический расчет, коррекция, экспорт
- ✅ **Обратная связь** - анонимные сообщения, файлы, статусы
- ✅ **Журнал действий** - отслеживание всех действий
- ✅ **Отчеты и аналитика** - KPI, статистика
- ✅ **Telegram бот** - регистрация, уведомления, команды, интерактивные кнопки
- ✅ **Push-уведомления** - Web Push API для браузерных уведомлений

## Обмен сменами

Система позволяет сотрудникам обмениваться сменами:

1. **Создание запроса** - сотрудник выбирает смену и коллегу для обмена
2. **Ответ коллеги** - принять или отклонить запрос
3. **Одобрение менеджера** - финальное подтверждение обмена
4. **Уведомления** - все участники получают уведомления на каждом этапе

### Права доступа
- `REQUEST_SHIFT_SWAP` - право запрашивать обмен (по умолчанию у всех сотрудников)
- `APPROVE_SHIFT_SWAP` - право одобрять обмены (менеджеры)

## Telegram интеграция

### Возможности
- **Регистрация сотрудников** - через пригласительные ссылки
- **Привязка аккаунта** - связь существующего аккаунта с Telegram
- **Отвязка аккаунта** - через API или личный кабинет
- **Уведомления** - о сменах, задачах, обменах и других событиях
- **Команды бота**:
  - `/start` - начало работы
  - `/today` - смены на сегодня
  - `/week` - смены на неделю
  - `/swaps` - активные запросы обмена
  - `/tasks` - мои задачи
  - `/settings` - настройки уведомлений
- **Интерактивные кнопки** - принять/отклонить обмен прямо в Telegram

### Настройка бота
1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Добавьте токен в `.env`:
   ```
   TELEGRAM_BOT_TOKEN=your_bot_token
   ```
3. Перезапустите backend

### API для Telegram
- `GET /api/auth/telegram/status` - статус привязки Telegram
- `POST /api/auth/telegram/unlink` - отвязать Telegram аккаунт
- `POST /api/invite-links/bind-telegram` - создать ссылку для привязки Telegram

## Структура проекта

```
resto.ydns.eu/
├── backend/          # Backend API
│   ├── src/
│   │   ├── controllers/
│   │   ├── routes/
│   │   ├── middleware/
│   │   └── utils/
│   └── dev.db        # SQLite база данных
├── frontend/         # React приложение
│   └── src/
│       ├── pages/
│       ├── components/
│       └── utils/
└── README.md
```

## Команды разработки

### Backend
```bash
cd backend
npm run dev          # Запуск в режиме разработки
npm run build        # Сборка
npm run db:init      # Инициализация БД (создание таблиц и начальных данных)
```

### Frontend
```bash
cd frontend
npm run dev          # Запуск в режиме разработки
npm run build        # Сборка для продакшена
npm run preview      # Предпросмотр продакшен сборки
```

## API Endpoints

- `/api/auth` - Авторизация
- `/api/shifts` - Смены
- `/api/shift-templates` - Типы смен
- `/api/swaps` - Обмен сменами
- `/api/tasks` - Задачи
- `/api/timesheets` - Табели
- `/api/feedback` - Обратная связь
- `/api/action-logs` - Журнал действий
- `/api/analytics` - Аналитика
- `/api/restaurants` - Рестораны
- `/api/permissions` - Управление правами доступа
- `/api/invite-links` - Пригласительные ссылки для Telegram бота
- `/api/notifications` - Уведомления

## 📚 Документация

Подробная документация находится в папке [`docs/`](docs/README.md):

- **Система прав доступа** - архитектура, использование, план реализации
- **Обмен сменами** - процесс обмена, статусы, уведомления
- **Telegram бот** - быстрый старт, настройка, команды
- **Мульти-ресторанность** - работа в нескольких ресторанах, права доступа

## База данных

Проект использует PostgreSQL. Для настройки:

```bash
# macOS
brew install postgresql@14
brew services start postgresql@14
createdb resto

# Применить схему
psql -d resto -f backend/src/database/scripts/postgresql-schema.sql
```

Настройте `DATABASE_URL` в `backend/.env`:
```
DATABASE_URL="postgresql://username@localhost:5432/resto"
```

## Лицензия

MIT
