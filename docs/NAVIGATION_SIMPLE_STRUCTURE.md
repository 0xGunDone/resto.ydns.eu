# Простая и логичная структура навигации

## Концепция: 3 уровня меню

### Уровень 1: Главное (всегда видно)
- 📊 **Главная** - дашборд со статистикой

### Уровень 2: Рабочие разделы (для сотрудников ресторана)
Эти разделы показываются, если пользователь работает хотя бы в одном ресторане:

- 📅 **График** - просмотр и управление графиком смен
- ✅ **Задачи** - управление задачами
- 📋 **Табели** - просмотр табелей и зарплаты

### Уровень 3: Управление (для менеджеров/админов)
- 🏢 **Рестораны** - список ресторанов (OWNER/ADMIN)
- ⚙️ **Управление рестораном** - сотрудники, должности, отделы (если есть права)

---

## Логика отображения

### Правило 1: Автоматический выбор ресторана

```
ЕСЛИ пользователь работает только в 1 ресторане:
  → Автоматически выбираем этот ресторан
  → Показываем все доступные разделы сразу

ЕСЛИ пользователь работает в нескольких ресторанах:
  → Показываем селектор ресторанов
  → Разделы для выбранного ресторана

ЕСЛИ пользователь OWNER/ADMIN:
  → Показываем селектор всех ресторанов
  → Все разделы доступны для выбранного ресторана
```

### Правило 2: Фильтрация по правам

```
Для каждого пункта меню:
  ЕСЛИ OWNER или ADMIN:
    → Показать всегда
  
  ИНАЧЕ:
    → Проверить права доступа
    → Показать только если есть хотя бы одно из требуемых прав
```

### Правило 3: Контекст ресторана

```
Разделы, требующие контекст ресторана:
  - График
  - Задачи
  - Табели
  - Управление рестораном

Эти разделы показываются ТОЛЬКО если:
  - Выбран ресторан ИЛИ
  - Пользователь работает только в одном ресторане
```

---

## Визуальная структура

### Десктоп (горизонтальное меню)

```
┌─────────────────────────────────────────────────────────────┐
│ 🏢 Resto    [Главная] [График] [Задачи] [Табели] [Рестораны ▼] │
│                                                              │
│                    [Ресторан: Ла Пиацца ▼]                  │
└─────────────────────────────────────────────────────────────┘
```

### Мобильное (выдвижное меню)

```
┌──────────────────┐
│ ☰  🏢 Resto      │
├──────────────────┤
│ 📊 Главная       │
├──────────────────┤
│ [Ресторан ▼]     │
│   • Ла Пиацца    │
├──────────────────┤
│ 📅 График        │
│ ✅ Задачи        │
│ 📋 Табели        │
├──────────────────┤
│ 🏢 Рестораны     │
│ ⚙️ Управление ▼  │
│   • Сотрудники   │
│   • Должности    │
└──────────────────┘
```

---

## Примеры для разных ролей

### EMPLOYEE (работает в 1 ресторане)
```
✅ Главная
✅ График (автоматически для своего ресторана)
✅ Задачи (только свои)
✅ Табели (только свои)
❌ Рестораны (нет доступа)
❌ Управление (нет доступа)
```

### MANAGER (работает в 1 ресторане)
```
✅ Главная
✅ График (для своего ресторана)
✅ Задачи (все задачи ресторана)
✅ Табели (все табели ресторана)
✅ Управление рестораном (для своего ресторана)
❌ Рестораны (нет доступа)
```

### OWNER (все рестораны)
```
✅ Главная
✅ Рестораны (селектор всех ресторанов)
✅ График (для выбранного ресторана)
✅ Задачи (для выбранного ресторана)
✅ Табели (для выбранного ресторана)
✅ Управление (для выбранного ресторана)
```

---

## Реализация: Простой подход

### Шаг 1: Определить контекст ресторана

```typescript
// Автоматически определяем ресторан
const getRestaurantContext = () => {
  // Если только 1 ресторан - автоматически выбираем
  // Если несколько - используем селектор
  // Если OWNER/ADMIN - используем селектор всех ресторанов
}
```

### Шаг 2: Группировать пункты меню

```typescript
const menuGroups = {
  main: ['Главная'],
  work: ['График', 'Задачи', 'Табели'], // Требуют ресторан
  management: ['Рестораны', 'Управление'], // Для админов/менеджеров
}
```

### Шаг 3: Фильтровать по правам

```typescript
// Для каждого пункта проверяем права
const shouldShowItem = (item) => {
  if (OWNER || ADMIN) return true;
  if (!hasPermission(item.permissions)) return false;
  if (item.requiresRestaurant && !hasRestaurantContext) return false;
  return true;
}
```

---

## Рекомендации

1. **Простота** - минимальное количество уровней вложенности
2. **Ясность** - всегда видно, для какого ресторана разделы
3. **Логичность** - связанные разделы рядом
4. **Адаптивность** - работает на всех устройствах

